<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="custom-control custom-checkbox pb-4">
    <%= form.check_box :available, id: :available, class: 'custom-control-input' %>
    <label class="custom-control-label" for="available">Publicar na página "Contato"</label>
  </div>

  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :job_name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_area :description, id: :job_description %>
  </div>

  <div class="form-group">
    <%= form.label :link %>
    <%= form.text_field :link, id: :job_link, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-success' %>
  </div>

  <br>
<% end %>

<script>
  var simplemde = new SimpleMDE({
    spellChecker: false
  });
</script>

